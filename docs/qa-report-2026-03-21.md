# QA Audit Report — 2026-03-21

**Auditor:** QA Engineer (automated)
**Scope:** Pre-launch audit of `iranti-site` at `src/`
**Date:** 2026-03-21
**Build status:** PASS (11 static pages generated, no errors)

---

## Defect Table

| ID | Severity | Location | Issue | Recommended Fix |
|----|----------|----------|-------|-----------------|
| QA-01 | P1 | `src/components/GetStartedContent.tsx:236` | `iranti claude-setup .` (with path arg) is inconsistent with every other site occurrence (`iranti claude-setup`, no path). README shows `iranti claude-setup` as canonical; AGENTS.md lists `iranti claude-setup --scan [dir]` as a scan variant but not a bare path form. The site should use one consistent invocation. | Standardize to `iranti claude-setup .` if the path form is officially valid, or drop the `.` to match Install.tsx, Integrations.tsx, and integrations/page.tsx. Confirm with the CLI author which form is canonical. |
| QA-02 | P1 | `src/app/proof/page.tsx` metadata (lines 10, 14, 23, 53, 62) | Metadata descriptions say "16/16 adversarial conflict resolution" but the external benchmark B3 (labeled "Conflict Resolution") is 4/5, not 16/16. The 16/16 is the *internal* adversarial suite (4 suites × 4 tests). A reader scanning meta descriptions or structured data could conflate the two. The on-page text disambiguates this correctly, but OG/Twitter previews and JSON-LD do not. | Revise metadata to say "16/16 internal conflict suite" or "4 suites × 4 tests = 16/16" to distinguish from B3's 4/5. |
| QA-03 | P2 | `src/components/Proof.tsx:109` | The homepage Proof section Gauges show `Gauge value={16} total={16} label="Conflicts"` and `Gauge value={4} total={4} label="Consistency"`. These are the internal suite scores. B3 (external conflict resolution, 4/5) appears in the same section below. No label on the gauges disambiguates "internal" vs "external". A visitor seeing "16/16 Conflicts" and then "B3: Conflict Resolution 4/5" below may be confused about what the gauge measures. | Add a subtitle or footnote to the gauge group, e.g., "Internal adversarial suite", to make it clear these gauges are not from the same benchmark program as B3. |
| QA-04 | P2 | `src/components/Install.tsx:35` (claudeSteps) | `iranti claude-setup` has no path arg here. `GetStartedContent.tsx:236` shows `iranti claude-setup .`. These two canonical install flows on the same site diverge. | Pick one form and apply it everywhere. |
| QA-05 | P2 | `src/app/proof/page.tsx:265` | Section heading "Consistency validation — 4/4" introduces a `frameworkResults` table showing framework compatibility (Raw OpenAI, LangChain, CrewAI). The section label says "Framework Compatibility" in the aside label but the h2 says "Consistency validation — 4/4". These are logically distinct concepts (framework compatibility is not the same thing as write serialization consistency), and the dual use of the section creates ambiguity. | Either rename the h2 to match the section content (framework compatibility results) or separate write serialization consistency into its own section with data distinct from framework results. |
| QA-06 | P2 | All pages | No skip-to-main-content link exists anywhere. This is not required but is a standard accessibility affordance for keyboard and screen reader users. | Add a visually hidden `<a href="#main-content">Skip to main content</a>` at the top of the Nav component and apply `id="main-content"` to the `<main>` element on each page. |
| QA-07 | P2 | `src/components/Proof.tsx:270` | Link to `https://github.com/nfemmanuel/iranti-benchmarking` (the external benchmarking repo). This is a different repo from the main `iranti` repo. If this repo does not yet exist publicly or has no content, the link will 404. | Confirm this repo is public and contains content before launch. If not, replace with a note that methodology is in-progress or link to the main repo's bench_logs directory. |
| QA-08 | P3 | `src/app/docs/page.tsx:74` | The Staff architecture concept card description says: "five internal components: Librarian, Attendant, Archivist, Mediator, Attendant". "Attendant" appears twice; "Mediator" appears here but "Resolutionist" is the name used everywhere else (Nav.tsx, product/page.tsx, TheStaff.tsx). | Change "Mediator" to "Resolutionist" and fix the duplicate "Attendant" — the fifth component is "Resolutionist". |
| QA-09 | P3 | `src/components/Hero.tsx` | The Hero proof strip shows `{c3}/4` for "consistency validation" with a count-up animation targeting `4`. This correctly animates to `4/4`. No issue with the number, but there is no tooltip or label clarifying these are internal-suite results vs external benchmarks. | Low priority. Consider adding a small "internal suite" label or linking to `/proof` for disambiguation. (The "See proof" button nearby helps but the strip itself is bare.) |
| QA-10 | P3 | All pages | No `<meta name="viewport">` tag is explicitly set. Next.js 15 injects this automatically in the app router, so this is not a functional defect, but worth confirming it appears in the rendered HTML. | Verify in browser DevTools post-deploy that the viewport meta tag is present. |
| QA-11 | P3 | `src/components/Footer.tsx:66` | Footer links to `https://www.npmjs.com/package/@iranti/sdk`. If `@iranti/sdk` is not yet published to npm, this will 404. Same risk for `https://pypi.org/project/iranti/`. | Confirm both packages are published before launch. If not published, remove or disable these links. |

---

## Audit Findings by Section

### 1. Link Audit

**Internal routes** — all verified as existing page files:

| Route | File | Status |
|-------|------|--------|
| `/product` | `src/app/product/page.tsx` | OK |
| `/proof` | `src/app/proof/page.tsx` | OK |
| `/get-started` | `src/app/get-started/page.tsx` | OK |
| `/integrations` | `src/app/integrations/page.tsx` | OK |
| `/docs` | `src/app/docs/page.tsx` | OK |
| `/` | `src/app/page.tsx` | OK |

**Anchor links** (`#install`, `#proof`, `#integrations`) — these section IDs exist as `id="install"` in Install.tsx, `id="proof"` in Proof.tsx, `id="integrations"` in Integrations.tsx. They are homepage-internal anchors. No nav link currently points to them (nav links route to full pages instead). The Get Started page uses `#quick`, `#claude-code`, `#codex`, `#chat` which are valid in-page anchors. Status: no broken anchor links.

**GitHub URLs** — all point to `https://github.com/nfemmanuel/iranti` consistently across all files. One additional link to `https://github.com/nfemmanuel/iranti-benchmarking` in `Proof.tsx` (see QA-07).

**mailto** — `hello@iranti.dev` appears in `Contact.tsx` only. Consistent and plausible.

**External doc links** — the following GitHub blob paths are used and should resolve if the repo is public and files exist:
- `docs/guides/quickstart.md`
- `docs/guides/claude-code.md`
- `docs/guides/codex.md`
- `docs/guides/security-quickstart.md`

### 2. Command Accuracy Audit

Commands cross-checked against `C:\Users\NF\Documents\Projects\iranti\README.md` and `AGENTS.md`:

| Command (site) | Status | Notes |
|----------------|--------|-------|
| `npm install -g iranti` | OK | Confirmed in README line 235 |
| `iranti setup` | OK | Confirmed in README line 238 |
| `iranti run --instance local` | OK | Confirmed in README line 241 |
| `iranti project init . --instance local --agent-id my_agent` | OK | Confirmed in README line 493 (pattern match) |
| `iranti doctor --debug` | OK | Confirmed in README line 251 |
| `iranti doctor --instance local` | OK | Confirmed in README line 514 |
| `iranti claude-setup` | OK | Confirmed in README line 337, AGENTS.md line 656 |
| `iranti claude-setup .` | INCONSISTENT | README shows bare `iranti claude-setup`; AGENTS.md lists `--scan [dir]` variant but not a bare path form. See QA-01. |
| `iranti codex-setup` | OK | Confirmed in README line 347, AGENTS.md line 658 |
| `iranti chat` | OK | Confirmed in README line 376, AGENTS.md line 653 |
| `pip install iranti` | OK | Confirmed in README line 281 |
| `npm install @iranti/sdk` | OK | Confirmed in README line 284 |
| `codex -C /path/to/your/project` | OK | Confirmed in README line 348 |

### 3. Benchmark Claim Audit

| Claim | Location | Status | Notes |
|-------|----------|--------|-------|
| 20/20 cross-session | Hero.tsx, Proof.tsx (B2), proof/page.tsx | Consistent | Maps to B2: "5 researchers × 4 facts" |
| 16/16 conflict | Hero.tsx gauge, Proof.tsx gauge + conflictResults, proof/page.tsx | Internally consistent | This is the internal adversarial suite (4 suites × 4 tests). Distinct from B3 (4/5 external). See QA-02/03. |
| 4/4 consistency | Hero.tsx, Proof.tsx gauge, proof/page.tsx | Consistent | Refers to write serialization / framework compat |
| 4/5 B3 external | Proof.tsx externalBenchmarks only | Not in proof/page.tsx | B3 appears on homepage Proof component but NOT on the dedicated /proof page. The /proof page only shows the internal suite (16/16) and framework results. This may be intentional (the /proof page expanded structure doesn't replicate the homepage Proof section), but it means the external benchmark program section is absent from the full proof page. |
| B1: 16/16 Entity Fact Retrieval | Proof.tsx externalBenchmarks | OK | Well-described, consistent |
| B2: 20/20 Cross-Session Persistence | Proof.tsx externalBenchmarks | OK | Well-described, consistent |

**Discrepancy flagged:** The external benchmark section (B1, B2, B3) only appears in `src/components/Proof.tsx` (homepage). It does not appear in `src/app/proof/page.tsx`. The dedicated proof page omits the external benchmarking program section entirely. This means the most detailed proof content (with B3's honest 4/5 and the benchmarking repo link) is homepage-only.

### 4. Version Number Consistency

All version references are `v0.2.12`:

| Location | Value | Status |
|----------|-------|--------|
| `Nav.tsx:57` | `v0.2.12` | OK |
| `Hero.tsx:65` | `v0.2.12` | OK |
| `Footer.tsx:112` | `v0.2.12` | OK |
| `Install.tsx:201` (comment) | `v0.2.12` | OK |
| `GetStartedContent.tsx:455` (comment) | `v0.2.12` | OK |

No version stragglers found.

### 5. Navigation Completeness

**Nav.tsx links:** Product, Proof, Docs, Integrations. CTA: GitHub, Get Started. All route to valid pages.

**Footer.tsx links:** Product, Proof, Integrations, Get Started, Docs overview, Quickstart (GitHub), Claude Code guide (GitHub), Security (GitHub), GitHub, Issues, Discussions, Changelog, npm packages (iranti, @iranti/sdk), PyPI, AGPL license. All internal links verified. External GitHub links use consistent `nfemmanuel/iranti` slug.

**Nav/Footer presence per page:**

| Page | Nav | Footer |
|------|-----|--------|
| `/` (page.tsx) | Yes (direct import) | Yes (direct import) |
| `/product` | Yes | Yes |
| `/proof` | Yes | Yes |
| `/get-started` | Yes | Yes |
| `/integrations` | Yes | Yes |
| `/docs` | Yes | Yes |

**CTAs checked:** All CTA buttons link to real routes (`/get-started`, `/proof`, `/integrations`, `/product`) or valid external URLs.

### 6. Accessibility Quick Check

| Check | Status | Notes |
|-------|--------|-------|
| `<img>` alt attributes | N/A | No `<img>` tags found in any source file. All graphics are inline SVG. |
| SVG aria-hidden | PASS | Decorative SVGs have `aria-hidden="true"` consistently |
| Button aria-labels | PASS | Copy buttons: `aria-label="Copy install command"` / `aria-label="Copy"`. Menu toggle: `aria-label={open ? "Close menu" : "Open menu"}`. Theme toggle: `aria-label` set dynamically. |
| `lang` on `<html>` | PASS | `<html lang="en">` in `layout.tsx:51` |
| Skip-to-main link | MISSING | No skip-to-content link. See QA-06. |
| Keyboard focus indicators | Not audited | Requires live browser testing. |

### 7. Build Verification

```
npm run build
```

**Result:** PASS

```
Route (app)                                 Size  First Load JS
┌ ○ /                                    9.44 kB         113 kB
├ ○ /_not-found                            977 B         102 kB
├ ○ /docs                                2.28 kB         106 kB
├ ○ /get-started                         6.13 kB         110 kB
├ ○ /integrations                        2.28 kB         106 kB
├ ○ /product                              3.8 kB         107 kB
├ ○ /proof                               2.28 kB         106 kB
├ ○ /robots.txt                            138 B         101 kB
└ ○ /sitemap.xml                           138 B         101 kB
```

All 11 pages generated as static content. No TypeScript errors. No lint errors.

---

## Summary

| Severity | Count | Items |
|----------|-------|-------|
| P0 — blocks launch | 0 | — |
| P1 — fix before launch | 2 | QA-01 (claude-setup command inconsistency), QA-02 (proof metadata conflates 16/16 with conflict resolution) |
| P2 — nice to fix | 5 | QA-03, QA-04, QA-05, QA-06, QA-07 |
| P3 — note only | 4 | QA-08, QA-09, QA-10, QA-11 |

**Build is clean. Routes are complete. Version numbers are consistent. No broken internal links. The two P1 items are messaging/accuracy issues, not structural defects. The site is close to launch-ready but QA-01 and QA-02 should be resolved before go-live.**
